# 2026-05-21

## 重构与修复
- **P0 重构**：全局 click listener 修复、renderedContent 死代码删除、颜色常量统一到 `src/constants/colors.js`、Store 方法重命名 (`toggleNoteTodo` → `addNoteTodo`)、事件监听清理
- **书签导入解析器重写**：放弃 lxml DOM 解析（破坏书签 HTML），改用基于位置事件排序的纯文本扫描算法
- **Bug 修复**：新建笔记跳转第一篇（addNote 返回对象结构错误）、书签导入全部归入「未分类」（lxml 吞并 DT 节点 + beautifulsoup parser 选择错误）

## 功能新增
- **卡片大小切换**（按分组）：新增 `display_mode` 字段，大/小两种模式，小卡片 32px 图标 + 透明背景
- **书签导入预览弹窗**：选择文件后先解析统计再确认导入
- **笔记右键菜单新增删除**：目录页/弹出目录页右键笔记可直接删除

## UI 调整
- 摸鱼笔记目录页改为两栏纵向排列
- 水印放大、标题间距加大
- 小卡片样式反复调整（透明背景、图标效果、高清图标源）

## 图标来源
- 最终固定使用 `favicon.im`（国内可访问）
- `scrape.py` 和 `backup.py` 都指向 `favicon.im`

## 测试
- 后端 pytest 测试：60 个，覆盖全部 API CRUD + 书签解析 + 备份恢复
- 前端 vitest 测试：26 个，覆盖 Pinia Store + 颜色工具函数
- **总计 86 个测试全部通过**
- 测试无需创建临时文件（`sqlite:///:memory:` + `atexit` 清理）

## 项目清理
- 删除旧 YAML 数据文件（home.yaml、notes.yaml、note-content.yaml）
- 更新 `.gitignore`

## 架构讨论
- 评估了 Tauri 桌面壳 + 卡片大小切换的后端数据库迁移
- 预留了 `display_mode` 字段说明
